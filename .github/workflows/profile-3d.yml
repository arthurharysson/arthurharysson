name: GitHub-Profile-3D-Contrib

on:
  schedule: # Roda a cada 24 horas
    - cron: "0 0 * * *"
  workflow_dispatch: # Permite rodar manualmente

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-3d-contrib
    steps:
      - uses: actions/checkout@v3
      - uses: yoshi38911/github-profile-3d-contrib@0.7.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          github_username: arthurharysson # <-- TROQUE AQUI
          
# Opcional: para gerar o gráfico com um ângulo diferente, como na imagem
#         use_perspective: true
#         perspective_angle: -60
#         perspective_x: 50
#         perspective_y: 50